# ── core stack ────────────────────────────────────────────────────────────────
fastapi>=0.110,<1.0
uvicorn[standard]>=0.27,<1.0
pydantic>=2.5,<3.0
websockets>=12,<17
httpx>=0.27,<1.0

# ── data + numerics ──────────────────────────────────────────────────────────
numpy>=1.26,<3.0
pandas>=2.1,<3.0          # NOTE: pandas 3.x has breaking API changes
scipy>=1.11,<2.0

# ── market data ──────────────────────────────────────────────────────────────
yfinance>=0.2.40
alpaca-py>=0.20

# ── config + parsing ─────────────────────────────────────────────────────────
python-dotenv>=1.0
beautifulsoup4>=4.12
lxml>=5.0

# ── optional heavy modules (still required at import time) ───────────────────
hmmlearn>=0.3

# ── rate limiting (security) ─────────────────────────────────────────────────
slowapi>=0.1.9

# ── optional: social sentiment (X/Twitter) ───────────────────────────────────
# pip install twikit  (optional — not on PyPI in all environments)

# ── development ───────────────────────────────────────────────────────────────
ruff>=0.6
mypy>=1.10
